I"�<p>从这小节之后，小菜就不用IDLE里面写实例，将用<code class="language-plaintext highlighter-rouge">vsCode</code>来编写python代码。</p>

<p>Python 条件语句是通过一条或多条语句的执行结果（<code class="language-plaintext highlighter-rouge">True</code> 或者 <code class="language-plaintext highlighter-rouge">False</code>）来决定执行的代码块，在python中，没有<code class="language-plaintext highlighter-rouge">switch</code>语法。</p>

<h1 id="1if语法格式">1.if语法格式</h1>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="n">mode</span> <span class="o">=</span> <span class="bp">True</span>

<span class="k">if</span> <span class="n">mode</span> <span class="p">:</span>
    <span class="k">pass</span>
<span class="k">else</span><span class="p">:</span>
    <span class="k">pass</span>

</code></pre></div></div>
<p><code class="language-plaintext highlighter-rouge">注意：pass是一个空语句，一般用于占位符</code></p>

<h1 id="2if嵌套">2.if嵌套</h1>

<div class="language-python highlighter-rouge"><div class="highlight"><pre class="highlight"><code><span class="k">if</span> <span class="n">condition</span><span class="p">:</span>
    <span class="k">pass</span>
    <span class="k">if</span> <span class="n">condition1</span><span class="p">:</span>
        <span class="k">pass</span>
    <span class="k">elif</span> <span class="n">condition2</span><span class="p">:</span>
        <span class="k">pass</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">pass</span>
<span class="k">else</span><span class="p">:</span>
    <span class="k">if</span> <span class="n">condition3</span><span class="p">:</span>
        <span class="k">pass</span>
    <span class="k">else</span><span class="p">:</span>
        <span class="k">pass</span>
</code></pre></div></div>

:ET